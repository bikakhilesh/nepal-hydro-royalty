__pycache__/
*.py[cod]
.venv/
venv/
.env

# The operator income statement used by the reconciliation section is private
# input, not part of this repo. Point HYDRO_XLSX at it locally.
*.xlsx

# preview-only copy of the 3D map; the Pages workflow makes its own
terrain.html

# Working checkout the refresh workflow makes of the scalper repo. It carries its
# own .git, and the commit step runs `git add -A`, which would record it as an
# embedded-repo gitlink -- a broken pointer for anyone who clones this.
_scalper/
